This is a tool for running trust based stores in PHP using SQLite as the datastore and PHP as the language.

It has no security and is designed to be used where purchases will be made on the "honor system".
